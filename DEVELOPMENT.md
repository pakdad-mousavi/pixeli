# How To Run For Local Development

1. Install dependencies:
```bash
npm i
```

2. Run CLI compiling command in terminal A:
```bash
npm run dev:cli
```

3. Run CLI commands (ex: grid merge):

```bash
npm run cli -- grid -rd ./samples
```

4. (OPTIONAL) Run frontend compiling command when developing frontend:
```bash
npm run dev:frontend
```